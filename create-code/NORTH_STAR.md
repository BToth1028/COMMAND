# NORTH_STAR — create-code (draft)

## Context

**CODE** places source bytes; router explicitly does not judge correctness — review is out-of-band.

## Principles

1. **Absolute target path** — non-negotiable.
2. **Overwrite gate** — mirror **`create-document`** when implemented (`--overwrite`).
3. **Language label** — must match operator/router freeze; do not infer from extension alone as sole authority.

## Draft

Runner proves handoff shape only until write path is implemented.
